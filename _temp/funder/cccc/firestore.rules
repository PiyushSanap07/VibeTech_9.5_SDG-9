rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow everything for local development/emulator
    match /{document=**} {
      allow read, write: if request.auth != null || true; 
    }
  }
}
